# MONOLITH Go-Live Checklist (Phase 1 + Phase 2)

Date: 2026-02-20  
Scope: Production hardening gates before live rollout.

## Guardrails (Mandatory)
- Zero math alterations in core physics/truth pipeline for this phase.
- Do not modify:
  - `core/walker_engine.py`
  - `core/hadamard_fusion.py`
  - SVD logic paths
- Do not split `analysis/MONOLITH_VIZ.py` yet.
- Do not apply geometric smoothing to walker trajectories.
  - Path tortuosity is signal, not noise.

## Phase 1: Baseline Lock
- Baseline artifact exists:
  - `D:\belief-transformer\V3\monolith_cockpit_restored_exact.html`
- Baseline snapshot exists:
  - `analysis/regression/monolith_baseline_snapshot.json`
- Snapshot generated by:
  - `python analysis/monolith_regression.py snapshot`
- Snapshot captures:
  - SHA256
  - trace count
  - mode boundaries (Synthesis/Diagnostics/Analysis)
  - key trace-name counts
  - zone-label invariants
  - HoTT icon presence
  - HUD NMI extraction

## Phase 2: Readiness Gates
- Visual/structure regression:
  - `python analysis/monolith_regression.py check <candidate_html> --experiment-dir <exp_dir>`
- Must pass all:
  - no `Fault` label
  - canonical zones present: Bridge/Swamp/Tightrope/Void
  - mode boundaries unchanged from baseline
  - trace count unchanged from baseline
  - key trace-name counts unchanged from baseline
  - HoTT icons present (`⛔`, `≠`)
  - HUD NMI matches `validation.json` (`nmi` or fallback key)

## Optional Strict Freeze Gate
- Require exact byte match to baseline:
  - `python analysis/monolith_regression.py check <candidate_html> --exact-html`

## Operational Notes
- If output directory ACL blocks writes, render to repo root and validate there.
- Any failed gate blocks promotion.
- Any enhancement branch must run regression gate before merge.

